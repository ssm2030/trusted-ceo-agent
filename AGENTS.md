# 저장소 작업 규칙

이 파일은 이 저장소 전체에 적용한다. 승인된 설계와 보안 게이트는 유지하되, 구현 중 검증은 변경 범위에 맞게 최소화한다.

## 탐색

- 위치 탐색은 `rg`와 `rg --files`를 우선하고 필요한 줄만 읽는다.
- 상태가 바뀌지 않았다면 같은 파일, DOM, JSON, 로그를 다시 전체 출력하지 않는다.
- 전체 문서와 전체 로그는 최종 판단에 꼭 필요한 경우에만 읽는다.

## 구현과 빠른 검증

- 기능 변경은 RED 테스트, focused 실행, 최소 구현, focused GREEN 순서로 진행한다.
- Web focused 테스트는 `npm --prefix web run test:focused -- <test-file...>`를 사용한다.
- 변경 파일과 직접 관련된 Python 테스트 모듈만 `uv run --project plugin/trusted-ceo-agent --frozen --offline --no-sync python -m unittest <module...>`로 실행한다.
- 계약 변경은 `npm --prefix contracts/web-report run check`와 해당 Python 계약 테스트만 실행한다.
- TypeScript 공용 타입이나 server/client 경계를 바꾼 작업 단위가 끝날 때 typecheck를 한 번 실행한다.
- 같은 실패가 두 번 반복되면 재실행을 멈추고 가설, 증거, 배제 원인, 다음 판별 실험을 짧게 기록한 뒤 접근을 바꾼다.
- 실패하지 않은 장문의 출력은 보관하거나 상태 문서에 누적하지 않는다.

## 전체 완료 게이트

- 편집, 도구 호출, 진행 보고마다 전체 테스트나 빌드를 실행하지 않는다.
- 구현 범위가 끝났을 때 계약 검사, Python 전체 테스트, Web typecheck·lint·unit·build, Playwright를 각각 한 번 실행한다.
- 완료 게이트 뒤 동작 코드나 설정이 바뀐 경우에만 영향을 받은 게이트를 다시 실행한다.
- CI의 blocking 검증과 보안·신뢰 경계 검증은 생략하지 않는다.
- 로컬 훅은 전체 검증을 실행하지 않으며, 로그 저장 외의 자동 작업을 추가하지 않는다.

## 상태와 보고

- 상태 파일은 의미 있는 작업 단위 완료, 실제 중단, 인계 때만 갱신한다.
- 완료 보고는 실행 명령, 종료 코드, 핵심 판정만 남긴다.
- 의미 있는 답변은 `결론:` 요약 뒤 `다음 할 일:`의 `내가 할 일`과 `사용자가 할 일`을 적는다.
